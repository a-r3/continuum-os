# Generated Fixture Cases

The hook fixture runner generates isolated temporary workspaces for each case.

Static JSON fixture files are intentionally not stored here because each case needs dynamic project-root paths, controlled governance state, and invocation from different working directories.
