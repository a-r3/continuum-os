# Hook Fixture Tests

`run-fixtures.sh` runs deterministic local fixtures against the P00 hook scripts.

The fixtures test safe paths, destructive commands, governance write coverage, bootstrap exceptions, and Stop-hook idempotency. They do not register hooks or call network services.
